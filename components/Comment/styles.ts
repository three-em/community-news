import styled from 'styled-components';

const Wrapper = styled.div`
  background: #f6f6ef;
  margin: 1rem;
`;

const CommentText = styled.p`
  font-size: 1rem;
  margin-left: 0.4rem;
  padding: 0;
  margin: 0.5rem;
`;

export { Wrapper, CommentText };
